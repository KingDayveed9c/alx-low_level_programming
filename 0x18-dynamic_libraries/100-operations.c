#include "main.h"

/**
* main - addition function
*@a: operand
*@b: operand
* Return: sum of numbers
*/

int add(int a, int b)
{
	return (a + b);
}

int mul(int a, int b)
{
	return (a * b);
}

int sub(int a, int b)
{
	return (a - b);
}

int div(int a, int b)
{
	return (a / b);
}

int mod(int a, int b)
{
	return (a % b);
}
